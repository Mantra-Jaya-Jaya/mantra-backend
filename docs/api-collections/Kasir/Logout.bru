meta {
  name: Logout
  type: http
  seq: 99
}

post {
  url: {{baseUrl}}/logout
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

headers {
  Content-Type: application/json
  X-Client-Type: flutter
}

body:json {
  {
    "refresh_token": "{{refreshToken}}"
  }
}
