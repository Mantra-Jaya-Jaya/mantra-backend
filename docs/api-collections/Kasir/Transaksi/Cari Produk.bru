meta {
  name: Cari Produk
  type: http
  seq: 5
}

post {
  url: {{baseUrl}}/kasir/transaksi/produk
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "keyword": "nama_barang_atau_barcode"
  }
}
