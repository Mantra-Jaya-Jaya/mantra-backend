meta {
  name: 200
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/kasir/transaksi/produk
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "keyword": "nama_barang_atau_barcode"
  }
}
