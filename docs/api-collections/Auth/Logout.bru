meta {
  name: Logout
  type: http
  seq: 4
}

post {
  url: {{baseUrl}}/logout
  body: json
  auth: bearer
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "refresh_token": "isi_refresh_token"
  }
}
