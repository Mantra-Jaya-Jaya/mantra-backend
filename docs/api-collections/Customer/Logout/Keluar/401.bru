meta {
  name: 401
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/logout
  body: json
  auth: bearer
}

auth:bearer {
  token: token-palsu-123
}

headers {
  Content-Type: application/json
  X-Client-Type: flutter
}

body:json {
  {
    "refresh_token": "{{refreshToken}}"
  }
}
