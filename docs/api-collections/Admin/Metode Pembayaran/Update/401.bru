meta {
  name: 401
  type: http
  seq: 2
}

put {
  url: {{baseUrl}}/admin/metode-pembayaran/:publicId
  body: json
  auth: bearer
}

params:path {
  publicId: {{metode_pembayaran_id}}
}

auth:bearer {
  token: token-palsu-123
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "urutan": 1
  }
}
