meta {
  name: 200
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/admin/karyawan/upload
  body: multipartForm
  auth: bearer
}

body:multipart-form {
  foto: @file(../../dummy.jpg)
}

auth:bearer {
  token: {{accessToken}}
}
