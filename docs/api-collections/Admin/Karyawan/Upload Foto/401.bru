meta {
  name: 401
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/admin/karyawan/upload
  body: multipartForm
  auth: bearer
}

auth:bearer {
  token: token-palsu-123
}

body:multipart-form {
  foto: @file(../../dummy.jpg)
}
