meta {
  name: 401
  type: http
  seq: 2
}

delete {
  url: {{baseUrl}}/admin/karyawan/:publicId
  body: none
  auth: bearer
}

params:path {
  publicId: {{karyawan_public_id}}
}

auth:bearer {
  token: token-palsu-yang-salah-123
}
