meta {
  name: 400
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/admin/karyawan
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "nama_lengkap": "Test Role Invalid",
    "username": "roleinvalid{{$randomInt}}",
    "email": "roleinvalid{{$randomInt}}@mantra.web.id",
    "password": "password123",
    "role": "SuperAdmin"
  }
}
