meta {
  name: Logout
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/logout
  body: json
  auth: bearer
}

headers {
  Content-Type: application/json
  X-Client-Type: nextjs
}

body:json {
  {
    "refresh_token": "{{refreshToken}}"
  }
}
