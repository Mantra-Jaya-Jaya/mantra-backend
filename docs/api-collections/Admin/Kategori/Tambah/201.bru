meta {
  name: 201
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/admin/kategori
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "nama_kategori": "Minuman"
  }
}
