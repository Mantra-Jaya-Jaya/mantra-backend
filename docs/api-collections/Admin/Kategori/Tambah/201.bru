meta {
  name: 201
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/admin/kategori
  body: json
  auth: bearer
}

headers {
  Content-Type: application/json
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "nama_kategori": "Jomok"
  }
}
