meta {
  name: 401
  type: http
  seq: 2
}

put {
  url: {{baseUrl}}/admin/ekspedisi/layanan/:id
  body: json
  auth: bearer
}

params:path {
  id: {{layanan_id}}
}

auth:bearer {
  token: token-palsu-123
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "deskripsi": "update"
  }
}
