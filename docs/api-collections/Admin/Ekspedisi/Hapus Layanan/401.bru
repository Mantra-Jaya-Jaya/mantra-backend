meta {
  name: 401
  type: http
  seq: 2
}

delete {
  url: {{baseUrl}}/admin/ekspedisi/layanan/:id
  body: none
  auth: bearer
}

params:path {
  id: {{layanan_id}}
}

auth:bearer {
  token: token-palsu-123
}
