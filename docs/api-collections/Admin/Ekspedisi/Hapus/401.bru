meta {
  name: 401
  type: http
  seq: 2
}

delete {
  url: {{baseUrl}}/admin/ekspedisi/:publicId
  body: none
  auth: bearer
}

params:path {
  publicId: {{ekspedisi_public_id}}
}

auth:bearer {
  token: token-palsu-123
}
