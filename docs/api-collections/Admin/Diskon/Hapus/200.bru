meta {
  name: 200
  type: http
  seq: 1
}

delete {
  url: {{baseUrl}}/admin/diskon/:publicId
  body: none
  auth: bearer
}

params:path {
  publicId: {{diskon_public_id}}
}

auth:bearer {
  token: {{accessToken}}
}
