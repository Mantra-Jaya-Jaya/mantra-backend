meta {
  name: 401
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/admin/diskon/upload
  body: multipartForm
  auth: bearer
}

auth:bearer {
  token: token-palsu-123
}

body:multipart-form {
  banner: @file(../../dummy.jpg)
}
