meta {
  name: 200
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/admin/diskon/upload
  body: multipartForm
  auth: bearer
}

body:multipart-form {
  banner: @file(../../dummy.jpg)
}

auth:bearer {
  token: {{accessToken}}
}
