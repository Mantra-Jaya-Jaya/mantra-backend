meta {
  name: 401
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/logout
  body: json
  auth: bearer
}

headers {
  Content-Type: application/json
  X-Client-Type: flutter
}

body:json {
  {
    "refresh_token": "{{refreshToken}}"
  }
}

auth:bearer {
  token: token-palsu-123
}
