meta {
  name: 200
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/admin/barang/upload
  body: multipartForm
  auth: bearer
}

body:multipart-form {
  gambar: @file(../../dummy.jpg)
}

auth:bearer {
  token: {{accessToken}}
}
