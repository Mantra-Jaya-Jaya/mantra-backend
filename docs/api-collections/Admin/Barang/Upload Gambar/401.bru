meta {
  name: 401
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/admin/barang/upload
  body: multipartForm
  auth: bearer
}

auth:bearer {
  token: token-palsu-123
}

body:multipart-form {
  gambar: @file(../../dummy.jpg)
}
